@page "/"
@inject TestBedViewModel ViewModel;


@*<Carlton.Dashboard.Components.ToDos.ToDoListCard ViewModel="ViewModel.ComponentViewModel"/>*@

@*<TestBed ComponentType="ViewModel.TestComponentType" 
        ViewModel="ViewModel.TestComponentViewModel"/>*@
         @*ComponentEventCallback="TestBedService.ComponentEventHandler"/>*@

<style>
    body {
        background-color: #f7f7f7;
    }
</style>


@code{

    //[Parameter]
    //public TestBedViewModel ViewModel { get; set; }

    public Type TestComponentType { get; set; }

    protected override void OnInitialized()
    {

        System.Console.WriteLine(Newtonsoft.Json.JsonConvert.SerializeObject(ViewModel));
        base.OnInitialized();




        //TestBedService.ComponentEventFired += (sender, args) =>
        //{

        //};

        //TestBedService.ComponentViewModelChanged += (sender, args) =>
        //{
        //    this.StateHasChanged();
        //};
    }
}
