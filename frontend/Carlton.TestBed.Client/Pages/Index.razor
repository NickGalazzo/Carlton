@page "/"
@inject TestBedService TestBedService
@inject CarltonTreeViewModel vm


<TestBed ComponentType="TestBedService.CarltonTreeViewModel.SelectedItem.Type" @bind-ViewModel="TestBedService.CarltonTreeViewModel.SelectedItem.ViewModel" />

<style>
    body {
        background-color: #f7f7f7;
    }
</style>


@code{
    public Type TestComponentType { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        TestBedService.ComponentViewModelChanged += (sender, args) =>
        {
            this.StateHasChanged();
        };
    }
}
