@using Carlton.Base.Client.State.Contracts
@using Carlton.Base.Client.Status

@namespace Carlton.Base.Client.State.Components

@typeparam TViewModel

@implements ICarltonComponent<TViewModel>
@implements IStatusComponent

@code {
    [Parameter]
    public TViewModel ViewModel { get; set; }
    [Parameter]
    public EventCallback OnComponentEvent { get; set; }
    [Parameter]
    public ComponentStatus ComponentStatus { get; set; }
}
